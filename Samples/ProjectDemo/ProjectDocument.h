#import <Cocoa/Cocoa.h>

/**
 * ProjectDocument is the NSDocument subclass which are the documents
 * from the NSDocument architecture's point of view.
 *
 * From the user's point of view the project is a container stored in a
 * bundle on disk containing the actual documents.
 */
@interface ProjectDocument : NSDocument
{
}

@end
